{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "coffee.brew_recommendations",
  "title": "Brew Recommendations",
  "description": "Value of the coffee.brew_recommendations metafield — an ordered list of brew method + description pairs.",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "required": ["method", "description"],
    "additionalProperties": false,
    "properties": {
      "method": {
        "type": "string",
        "minLength": 1,
        "description": "Brew method name, e.g. 'Pour Over', 'French Press', 'Espresso'."
      },
      "description": {
        "type": "string",
        "minLength": 1,
        "description": "Short brewing guidance for this method."
      }
    }
  }
}
